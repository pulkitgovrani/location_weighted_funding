const sendNotification = (userId, message) => {
  // This function would integrate with a real notification service.
  console.log(`Notifying user ${userId}: ${message}`);
};

module.exports = { sendNotification };
